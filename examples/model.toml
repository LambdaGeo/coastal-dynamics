# examples/coastal_model.toml

[model.parameters]
end_time      = 20
taxa_elevacao = 0.5
altura_mare   = 6.0
acrecao_ativa = false
resolution    = 100.0
crs           = "EPSG:31984"
bands         = ["uso"]

# Interactive mode — set to true for local CLI with display
interactive   = false